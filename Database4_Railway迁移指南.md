# 📊 Database 4.xlsx 转换到 Railway 数据库使用指南

## 🎯 目的
将 `database 4.xlsx` 文件中的所有数据迁移到新的Railway云数据库中，供FRP预测平台使用。

## 📋 前置准备

### 1. 确保文件存在
```bash
# 检查当前目录是否有 database 4.xlsx
ls "database 4.xlsx"
```

### 2. 准备Railway数据库
- 在 [Railway.app](https://railway.app) 创建新项目
- 添加MySQL数据库服务
- 获取数据库连接信息：
  - 主机地址 (例: `containers-us-west-xxx.railway.app`)
  - 端口 (通常是 `3306`)
  - 用户名 (通常是 `root`)
  - 密码 (Railway自动生成)
  - 数据库名 (通常是 `railway`)

## 🚀 迁移步骤

### 一键迁移命令：
```bash
python database4_to_railway.py
```

### 详细流程：
1. **运行脚本**
   ```bash
   python database4_to_railway.py
   ```

2. **输入Railway数据库信息**
   ```
   数据库主机: containers-us-west-xxx.railway.app
   数据库端口: 3306
   数据库用户名: root
   数据库密码: [Railway生成的密码]
   数据库名称: railway
   ```

3. **等待迁移完成**
   - 自动读取Excel文件（132个字段）
   - 清理和处理数据
   - 分批上传到Railway
   - 验证数据完整性

4. **检查生成的配置文件**
   - `.env` - 本地开发用配置
   - `railway_streamlit_secrets.toml` - Streamlit Cloud用配置

## 📊 迁移内容

### 数据范围：
- **文件**: `database 4.xlsx`
- **字段数**: 132个完整字段
- **数据行数**: Excel文件中的所有有效行
- **表名**: `research_data`

### 字段映射：
包含所有重要字段，如：
- 基本信息: Title, Author, Year, Journal
- 材料信息: Fiber_type, Matrix_type, 直径等
- 环境条件: 温度, 湿度, pH值等
- 测试结果: 强度保留率, 质量变化等
- 参考信息: DOI, 备注等

## ✅ 成功标志

迁移成功后您会看到：
```
🎉 Database 4.xlsx 迁移到Railway成功完成！

📁 生成的文件:
  ├── .env (本地开发配置)
  └── railway_streamlit_secrets.toml (Streamlit Cloud配置)

📊 Railway数据库中共有 XXXX 条记录
```

## 🔧 配置应用

### 本地测试：
```bash
# 使用生成的 .env 文件
streamlit run app.py
```

### Streamlit Cloud部署：
1. 复制 `railway_streamlit_secrets.toml` 的内容
2. 粘贴到Streamlit Cloud的Secrets配置中
3. 重新部署应用

### 示例Secrets配置：
```toml
DB_HOST = "containers-us-west-xxx.railway.app"
DB_PORT = "3306"
DB_NAME = "railway"
DB_USER = "root"
DB_PASSWORD = "your-railway-password"
SECRET_KEY = "frp-railway-20251105-secret"
```

## 🔍 验证数据

迁移完成后，脚本会自动显示：
- 总记录数量
- 样本数据预览
- 统计信息（纤维类型数量、作者数量等）

## ❓ 常见问题

### Q1: Excel文件找不到
```bash
❌ 找不到文件: database 4.xlsx
```
**解决**: 确保 `database 4.xlsx` 在脚本所在目录

### Q2: Railway连接失败
```bash
❌ Railway数据库连接失败
```
**解决**: 
- 检查Railway数据库是否正常运行
- 确认连接信息正确
- 检查网络连接

### Q3: 数据插入失败
```bash
❌ 数据迁移失败
```
**解决**:
- 检查Railway数据库存储空间
- 确认数据库权限
- 重试迁移

## 📞 技术支持

如遇问题，请检查：
1. Excel文件格式和内容
2. Railway数据库状态和连接信息
3. 网络连接稳定性
4. 脚本日志输出中的具体错误信息

---
**注意**: 迁移过程中会清空目标表的现有数据，请确保备份重要数据！